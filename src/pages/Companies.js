import React from 'react';
import CompaniesList from '../components/CompaniesList';

const Companies = () => {
  return (
    <div>
      <CompaniesList />
    </div>
  );
};

export default Companies;